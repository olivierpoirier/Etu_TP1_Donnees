

export const regexName = new RegExp(/^[A-Za-z ]{3,50}$/);
export const regexPrice = new RegExp(/^[1-9]\d*(\.\d+)?$/);
export const regexQuantity = new RegExp(/^[1-9]\d*$/);

export const regexEmail = new RegExp(/^[\w\.-]+@[a-zA-Z\d\.-]+\.[a-zA-Z]{2,6}$/);
